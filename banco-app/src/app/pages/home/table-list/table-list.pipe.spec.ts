import { TableListPipe } from './table-list.pipe';

describe('TableListPipe', () => {
  it('create an instance', () => {
    const pipe = new TableListPipe();
    expect(pipe).toBeTruthy();
  });
});
